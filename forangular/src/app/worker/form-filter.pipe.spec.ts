import { FormFilterPipe } from './form-filter.pipe';

describe('FormFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FormFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
